[package]
name = "skb-core"
version = "0.1.0"
edition = "2021"
rust-version.workspace = true
description = "Surreal Knowledge Base - core library"

[features]
default = []
ort = ["dep:ort", "dep:ndarray"]

[dependencies]
surrealdb = { version = "3", default-features = false, features = ["kv-surrealkv"] }
tokenizers = { version = "0.23", default-features = false, features = ["onig", "progressbar"] }
hf-hub = { version = "1", features = ["blocking"] }
ort = { version = "2.0.0-rc.13", default-features = false, features = [
  "std", "ndarray", "tracing", "download-binaries", "tls-rustls",
  "copy-dylibs", "api-27",
], optional = true }
ndarray = { version = "0.17", optional = true }
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
schemars = { workspace = true }
serde_json = "1"
thiserror = "2"
toml = "1.1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
sha2 = "0.11"
anyhow = "1"
# Pinned exactly: SafeResolver uses ureq::unversioned (not semver-guaranteed).
# Re-audited 2026-08-24: SafeResolver+with_parts still pins validated IPs on 3.4.0;
# RequestExt seal (3.4.0 breaking) is N/A (we use Resolver, not RequestExt).
ureq = "=3.4.0"
url = "2"
base64 = "0.23"
regex = "1"
pdf-extract = "0.12"
lopdf = "0.44"

[dev-dependencies]
criterion = "0.8"
ahash = "0.8"

[[bench]]
name = "skb"
harness = false
